"""Lane model -- base class for Track and Channel."""

from .referenceable import Referenceable


class Lane(Referenceable):
    """Base class for structural elements (Track, Channel) in the project."""
    pass
